Flask==2.3.3
Flask-CORS==4.0.0
Flask-Limiter==3.5.0
PyPDF2==3.0.1
pdf2docx==0.5.8
Pillow==10.4.0
# PyMuPDF (fitz) is optional at runtime and can be installed separately.
# Commented out to unblock installs on Windows when no matching wheel is available.
# Install later with a matching wheel for your Python version (see instructions).
# PyMuPDF==1.24.0
pdfminer.six==20221105
pdfplumber==0.10.2
python-dotenv==1.0.0
requests==2.31.0
Werkzeug==2.3.7
openai==0.28.1
anthropic==0.25.0
pytesseract==0.3.10
reportlab>=3.6.13
pydantic>=2.0.0
pydantic-settings>=2.0.0
pytest>=7.0.0
pytest-mock>=3.10.0
marshmallow>=3.20.0
python-magic-bin==0.4.14; sys_platform == "win32"
python-magic==0.4.27; sys_platform != "win32"
wrapt>=1.14.1
# Lightweight alternatives for heavy packages
# openpyxl==3.1.2  # Commented out - can cause issues
# pandas and numpy commented out - can cause compatibility issues